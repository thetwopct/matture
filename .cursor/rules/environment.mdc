---
description:
globs:
alwaysApply: false
---
The environment this plugin is developed under uses WP-ENV to launch the plugin in its own Docker installation.

If running CLI commands, these must be run like this:

```
wp-env run cli --env-cwd=wp-content/plugins/plugin-name wp config get
```
